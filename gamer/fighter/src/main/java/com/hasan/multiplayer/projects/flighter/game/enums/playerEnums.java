package com.hasan.multiplayer.projects.flighter.game.enums;

public class playerEnums {
    public enum playerType {
        player,
        ai,
        remotePlayer;
    }
    public enum playerFuelType {
        mana,
        power,
        health
    }
    public enum playerDirection {
        right,
        left
    }
}
